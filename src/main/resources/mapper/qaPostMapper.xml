<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.moa.mapper.qapost.QaPostMapper">
    <insert id="insert">
        INSERT INTO TBL_QA_POST(ID)
        VALUES (#{id})
    </insert>

    <select id="selectAll" resultType="qaPostDTO">
        SELECT QP2.ID, USER_ID, USER_MAJOR, QP2.UPDATED_DATE
        FROM
            (
            SELECT ROWNUM R, QP1.ID, USER_ID, USER_MAJOR, QP1.UPDATED_DATE
            FROM
                (
                SELECT U.ID, QP.USER_ID, USER_MAJOR, QP.UPDATED_DATE
                FROM TBL_USER U JOIN (
                    SELECT QP0.ID, POST_TITLE, POST_CONTENT, USER_ID, POST_VIEW, UPDATED_DATE
                    FROM TBL_POST P JOIN TBL_QA_POST QP0
                    ON P.ID = QP0.ID
                ) QP
                ON U.ID = QP.USER_ID
                ORDER BY QP.ID DESC
                ) QP1
                    <![CDATA[
                    WHERE ROWNUM <= #{endRow}
                    ) QP2
                    WHERE ROWNUM >= #{startRow}
                    ]]>
    </select>


    <select id="selectCount">
        SELECT COUNT(*) FROM TBL_QA_POST
    </select>

    <update id="updateById">
        UPDATE TBL_QA_POST
        SET PROJECT_FIELD = #{projectField},
            PROJECT_REQUIREMENT = #{projectRequirement}
        WHERE ID = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM TBL_QA_POST
        WHERE ID = #{id}
    </delete>
</mapper>