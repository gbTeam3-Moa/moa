<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.moa.mapper.projectpost.ProjectPostMapper">

    <insert id="insert">
        INSERT INTO TBL_PROJECT_POST(ID, PROJECT_FIELD, PROJECT_REQUIREMENT)
        VALUES (SEQ_PROJECT_POST.NEXTVAL, #{projectField}, #{projectRequirement})
    </insert>

    <select id="selectById" resultType="projectPostDTO">
        SELECT ID, PROJECT_FIELD, PROJECT_REQUIREMENT
        FROM TBL_PROJECT_POST
        WHERE ID = #{id}
    </select>

    <update id="updateById">
        UPDATE TBL_PROJECT_POST
        SET PROJECT_FIELD = #{projectField},
            PROJECT_REQUIREMENT = #{projectRequirement}
        WHERE ID = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM TBL_PROJECT_POST
        WHERE ID = #{id}
    </delete>

</mapper>
